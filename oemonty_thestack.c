#include "monty.h"

/**
 * f_stack - this is to Set the mode flag to indicate stack.
 * @head: Stack head
 * @counter: Line number
 * Return: Has no return
 */
void f_stack(stack_t **head, unsigned int counter)
{
	/*variable Declaration*/
	(void)head;
	(void)counter;
	bus.lifi = 0;
}
